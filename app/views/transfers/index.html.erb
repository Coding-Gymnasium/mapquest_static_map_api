<h1> Transfers </h1>
<%= link_to "Create a New Transfer", new_transfer_path %><br/>

<% @transfers.each do |transfer| %>
  <ul>
    <li><h3> <%= link_to Student.find(transfer.student_id).full_name, transfer_path(transfer.id) %></h3> </li>
  </ul>
<% end %>